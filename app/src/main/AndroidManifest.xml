<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.graduationproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".oferinstruction"></activity>
        <activity
            android:name=".nave"
            android:label="@string/title_activity_nave"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".places" />
        <activity android:name=".splachscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RecyclerViewCompany" />
        <activity android:name=".OferDetail" />
        <activity android:name=".login" />
        <activity android:name=".ConpanyDetail" />
        <activity android:name=".regestration" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>